# 三期批次 52 · 库容治理（拆分＋日志外置＋体量预算）

- **工人**：闲时2026-09-07　**状态**：✅ 2026-09-07
- **前置自检**：批次 49 已 ✅（ecd2a49 前之 c8f5403）；全库 `wc -l`/`du` 盘点已跑（脚本 `批次52_验证.py`／本文件 §盘点）。

## 一、盘点清单与拆分裁决

全库 md 扫描（>400 行或 >60KB 命中 23 文件）：**必须处理**＝`15_…/生产排期_填充批次_二期.md`（454 行/115KB，走工作项 1 日志外置）、`02_职业×流派×武器解耦.md`（669 行/66KB）、`18_/02_进阶套.md`（393 行/88KB，任务书点名）；**豁免**＝合集（脚本生成物）、`12` 号（常量唯一出处）、13 职业卡（已一对象一文件）。
**裁决（不入拆）**：`03_巨兽与Boss设计.md`（427 行/36KB）、`14_全局待裁决项与回填记录.md`（410 行/37KB）、`15_主线章节与难度阶段.md`（400 行/38KB）满足「≤400 行**或** ≤60KB」预算单边（纪律 10 为或关系，双超才超限）；且 14 号系批次 50 解锁登记处、03 号 §锚点被全库引用，拆分弊大于利。此裁决已同步写入 `11` 号 §11.8·补 预算条款口径。

## 二、工作项成果

1. **排期日志外置**：一期 16 条＋二期 31 条＝47 条长备注全文外置至 `生产日志/一期|二期/批次NN.md`（一批次一文件，含状态/日期/外置说明）；状态表备注压为 ≤40 字摘要。压缩后一期 29,944 字节、二期 63,484 字节（判据 ≤80KB ✓）。git diff 复核 47 行替换、行数不变、零内容丢失。
2. **02 号拆分**：`02_职业解耦/` 15 文件＝`00_总则与共用资源层.md`（478 行/49.9KB，含 §2.1–2.2.0＋2.2.15–2.6）＋ `01_战士_Warrior.md`…`14_弓手_Archer.md` 各一文件（15–25 行，逐字保真 diff 抽验）；原文件改索引页 29 行（分册结构表＋检索指引，≤80 行 ✓）。
3. **18_/02_进阶套拆分**：`02_进阶套/` 4 文件＝`00_总则与自检.md`（§一/§二/§三·0/§四/备注）＋巨兽系按星段三册 `01_巨兽系_★1-3.md`（59 行）/`02_巨兽系_★4-6.md`（95 行）/`03_巨兽系_★7-9.md`（174 行）；原文件改索引页 20 行（含总量验算 4＋12＋302＝318 套）。注：任务书所言「王骸／散件」实存 `03_王骸套与散件.md`（未超限，不拆），本册仅巨兽系按段三分为最贴近任务书口径的落法。
4. **11 号体量预算成文**：新增 §11.8·补「文件体量预算」（预算线/豁免/超限处置/日志外置/读取纪律/盘点义务六条款）。
5. **README 目录树同步**：02 号行改索引页注＋`02_职业解耦/` 分册行；15_ 块补 `任务文案/`（57 批遗漏）与 `生产日志/` 行；新增 `18_装备与素材图鉴/` 块（该目录此前整体缺席目录树，本批补一册行＋`02_进阶套/` 分册行）。
6. **合集重镜像**：`build_collection.py` 第 2 章改分册列表拼接（00＋01–14，剔重 00 重复 glob 的缺陷）＋剔除「拆分注记」导航块；重跑退出码 0，合集 15 章 4,483 行，第 2 章 671 行无重复、11 号新条款入镜。此重建顺带把 48–56 批对镜像内文件（12 号等）的既有改动一并入镜（生成物语义）。

## 三、完成判据实测（脚本 `批次52_验证.py`，全 PASS）

- ① 一期 29,944 B／二期 63,937 B 均 ≤80KB；备注 16＋31 条全 ≤40 字；生产日志一期 16 件／二期 31 件，批次 45 全文抽查在案。
- ② `02_职业解耦/` 15 文件齐；原 02 号索引页 29 行。
- ③ `02_进阶套/` 4 分册最大 174 行 ≤400。
- ④ `11` 号 §11.8·补 条款在案（含「≤400 行或 ≤60KB」口径）。
- ⑤ README 树含 `02_职业解耦/`、`02_进阶套/`、`生产日志/`、`任务文案/`；**本批改动文件内断链＝0**（全库 md 链接/路径引用程序化扫描）。

## 四、遗留登记（均非本批产物，留批次 51 终检裁决）

1. **旧合集名悬空引用 11 处**：`00/01/03–10 号` 章文件头仍引 `../云海猎团_QQ群回合制RPG_PvE设计稿.md`（合集改名前旧引用，指向不存在路径；我方新拆 `02_职业解耦/00` 内 1 处已就地校正，其余 11 处非本批范围未动）。
2. **库外历史出处注记 18 处**：19 卡 `/tmp/yunhai_chapters_source.md` 迁移痕迹、`14` 号 `/workspace/…` 前身工程与省略名文件——历史出处性质，非导航链接。
3. **规划/模板引用 3 处**：`17/00` 索引所列规划文档名 2 个（22/24 号规划页未建）＋skills 方法论 `NN_职业名_English.md` 占位——均在本批未触碰文件内。

## 五、并发事件（收尾时间线）

本批在途期间，批次 56 执行器收尾提交 `ecd2a49`，其 `git add -A` 按条款语义卷入我方在途产出（拆分文件/排期外置/11 号/首版脚本与合集）；其提交信息已注明「52 收尾自提增量」。本批按 58 轮先例「验产出在盘＋同一产出验收收尾」：在库版本经核验后，本提交自提增量＝README 目录树三处、`02_职业解耦/00` 旧合集名校正、`build_collection.py` 分册拼接修复（剔 00 重复 glob）、合集重镜像（剔第 2 章重复）、脚本移档至 `生产日志/三期/`、执行日志与状态表翻转。

## 六、脚本清单（本目录）

`批次52_备注外置.py`（47 条外置＋摘要）、`批次52_拆分.py`（02 号 15 分册＋进阶套 4 分册＋索引页）、`批次52_验证.py`（判据①–⑤＋断链扫描）。
