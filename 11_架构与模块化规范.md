# 《云海猎团 Cloudsea Hunting Corps》· 架构与模块化规范

> **本文定位**：全项目的**工程总纲**。它不描述玩法，而是规定玩法"怎么被装配起来"。
> 前 10 章回答「游戏是什么」，本文回答「任何一块被拿掉之后，剩下的还能不能跑」。
> 配套：`12_全局常量表.md`（所有数字的单一出处）。

---

## 11.0 三条铁律

| # | 铁律 | 判据（违反即打回） |
|---|---|---|
| **F1** | **内核最小化** | 内核层只做"推进回合 + 结算一次伤害 + 存读快照"三件事。任何**玩法概念**（职业、武器、异常、Boss、素材）都不许出现在内核里 |
| **F2** | **一切皆数据** | 逻辑代码里不得出现**具体数值**、**具体内容 ID**、**具体职业/武器/异常分支**。三者分别进常量表、枚举表、数据表 |
| **F3** | **单向依赖 + 事件总线** | 依赖只允许 `L2 内容 → L1 系统 → L0 内核`。同层模块之间**禁止直接调用**，一律通过事件总线广播/订阅 |

> **F1–F3 的收益**：拆除任意 L1 模块，内核照转，其余模块无感知；新增一个模块，不需要改动任何既有模块的一行代码，只需「注册表加一行 + 挂几个钩子」。

---

## 11.1 三层架构

```
┌─────────────────────────────────────────────────────────────┐
│  L2 内容层  Content（纯数据，热更，零逻辑）                    │
│  职业表 / 武器表 / 动作表 / 战技表 / 绝技表 / 异常表 /          │
│  Boss 表 / 部位矩阵表 / 素材表 / 套装表 / 掉落表 / 文本表        │
└──────────────────────────┬──────────────────────────────────┘
                           │ 只读引用（表 ID → 行）
┌──────────────────────────▼──────────────────────────────────┐
│  L1 系统层  Systems（可插拔模块，20+ 个，逐个可拆）             │
│  部件破坏 / 意图瞬御 / 压制 / 异常九态 / 共生灵 / 风缆 /        │
│  绝技 / 战技 / 流派 / 同行兽 / 符文 / 丰饶符 / 掉落 /           │
│  巨兽阶段机 / 托管AI / 指令UI / 叙事 / 据点 / 超限              │
└──────────────────────────┬──────────────────────────────────┘
                           │ 钩子注册 + 事件订阅
┌──────────────────────────▼──────────────────────────────────┐
│  L0 内核层  Core（不可拆，≈6 个部件，无玩法语义）               │
│  回合调度 / 结算管线 / 状态栈 / 事件总线 / 表加载 / 快照         │
└─────────────────────────────────────────────────────────────┘
```

**内核的"无玩法语义"检验法**：把内核单独拿去做一个五子棋或抽卡模拟器，若需要删掉任何一行才能复用，说明有玩法泄漏进内核，不合格。

---

## 11.2 模块注册表（Module Registry）

> 每个模块在系统启动时向注册表登记自身，声明**它订阅什么事件、往结算管线挂哪几个钩子、依赖哪些可选模块**。
> **注册表是唯一的装配点**——装/拆模块 = 注册表增删一行，不碰其他模块代码。

### L0 内核层（6 个，不可拆）

| ID | 部件 | 职责 | 不许包含 |
|---|---|---|---|
| `C01` | 回合与行动条调度 | 按行动值推进出手序列，产出出手事件 | 不许知道"职业/武器"是什么 |
| `C02` | 结算管线 | 执行**有序乘区链**（八步），产出最终伤害 | 不许内置任何具体乘区（乘区由 L1 注册） |
| `C03` | 状态与 Buff 栈 | 通用状态容器（层数、时长、来源、驱散优先级） | 不许内置"腐蚀""麻痹"等具体状态 |
| `C04` | 事件总线 | 发布/订阅、优先级、取消传播 | 不许内置任何业务事件名以外的逻辑 |
| `C05` | 表加载与常量解析 | 加载 L2 数据表 + `C.*` 常量解析、校验、热更 | 不许把表内容写进代码 |
| `C06` | 快照与异步续局 | 局状态序列化 / 跨天恢复 / 超时推进 | 不许知道"托管 AI"的策略内容 |

### L1 系统层（22 个，逐个可拆）

| ID | 模块 | 依赖（硬） | 可选依赖（软） | 可拆 | **拆除后系统如何降级** |
|---|---|---|---|---|---|
| `S01` | 部位硬度与部件破坏 | C02 C05 | S03 S15 | ✅ | 所有部位 H 取 1.0、破坏槽关闭；战斗退化为"打本体"，仍完整可玩 |
| `S02` | 意图预告与瞬御 | C01 C02 | S06 S07 | ✅ | 敌人不再预告，玩家无瞬御选项；指令位② 自动让位给绝技 |
| `S03` | 压制 Override | C02 C03 | S01 S06 | ✅ | 无倒地窗口；绝技窗口改由部件破坏（S01）或异常（S04）单独提供 |
| `S04` | 异常九态 | C02 C03 C05 | S05 S08 S15 | ✅ | 战斗退化为"属性 + 部位"二维；异常乘区 Xa 恒为 1.00 |
| `S05` | 共生灵 | C01 C03 | S04 | ✅ | 战前配置单少一行；孢子相关资源条目自动隐藏 |
| `S06` | 风缆与位移 | C01 | S02 S11 | ✅ | 站位固定；御风流相关条目自动改写为等效的数值补偿 |
| `S07` | 绝技 Arcana | C01 C02 C03 | S03 S04 | ✅ | 指令位② 移除；战技位扩为 3（原③位可放战技） |
| `S08` | 战技与战技位 | C01 C02 | S07 S09 S11 | ✅ | 每回合改为固定基础攻击（动作表默认行），最简模式 |
| `S09` | 职业资源 RU | C03 C05 | S08 S10 | ✅ | 所有职业改用通用能量条（0–100），绝技充能速率 +50% 补偿 |
| `S10` | 武器模式与 G×T 注入 | C05 | S09 | ✅ | 所有武器用同一节奏；武器差异仅保留 ATK 与射程 |
| `S11` | 五流派 | C05 | S06 S07 S08 | ✅ | 全员按均衡流（D 1.00）计算 |
| `S12` | 同行兽 | C01 C02 C03 | S13 S18 | ✅ | 队伍系数 1.6 → 1.1，敌人 EHP 按同比例缩放（自动，见 11.5） |
| `S13` | 装备与符文 | C05 | S14 S15 | ✅ | 角色属性 = 职业基础值 + 武器 ATK，成长只剩武器强化一线 |
| `S14` | 丰饶符与产出加成 | C05 | S15 | ✅ | 收益恒 ×1，无限刷；经济改由 S15 的掉落率单独承担节流 |
| `S15` | 掉落与素材 | C05 | S01 S13 | ✅ | 无素材产出，成长改为纯任务奖励直给（见降级表） |
| `S16` | 巨兽阶段状态机 | C01 C03 | S02 S17 | ✅ | 敌人无阶段转换，单阶段打完；TTK 上限自动压到 12 回合 |
| `S17` | 异相种 / 蚀化爬塔 | C05 | S16 | ✅ | 只剩常规空兽；长线内容由重复挑战 + 限时活动承担 |
| `S18` | 托管 AI 与超时预案 | C01 C06 | S08 S12 | ✅ | 超时不再自动推进，改为**暂停等待**（异步体验受损但不崩） |
| `S19` | 指令解析与 UI 战报 | C01 C05 | — | ⚠️ | 人机接口层，不可拆；但**表现模板可整替换**，不影响逻辑 |
| `S20` | 叙事与引导 | C05 | S21 | ✅ | 纯文本层，拆除后游戏仍完整可玩，只是无剧情 |
| `S21` | 云枢浮港据点 | C05 | S12 S13 S15 | ✅ | 战前配置改在战场入口一屏内完成，功能等价 |
| `S22` | 超限突破 | C05 | S13 | ✅ | 成长上限压到"觉醒"层；数值膨胀由版本迭代承接 |

> **可拆 ≠ 可随便拆**：`S19` 是人机接口，属于"必要核心功能"，故标 ⚠️。除此之外 `S01–S18`、`S20–S22` 全部可拆。

### L2 内容层（纯数据）

`职业表 / 武器表 / 基础动作表 / 战技表 / 绝技表 / 异常表 / 部位矩阵表 / Boss 表 / 意图表 / 素材表 / 套装表 / 掉落表 / 共生灵表 / 同行兽表 / 符文表 / 文本表`

**内容层的自由度**：任意一行数据的增删改，都不需要改代码；新增一个职业 = 在职业表加一行 + 在战技表加若干行。

---

## 11.3 依赖规则（可执行判据）

| 规则 | 说明 | 违反后果 |
|---|---|---|
| **R1 单向** | `L2 → L1 → L0`，禁止反向引用 | 内核被玩法污染，F1 失守 |
| **R2 同层零直调** | L1 模块 A 想影响 B，只能发事件，不能直接调 B 的函数 | 拆除 A 时 B 崩 |
| **R3 软依赖显式声明** | 用到别的模块，必须在注册表 `optional_deps` 声明 | 拆除后静默出错 |
| **R4 空安全** | 软依赖不存在时，模块走降级分支，**不得抛异常、不得静默返回 0** | 隐性崩溃 |
| **R5 内容不反向依赖系统** | 数据表里可以有"引用哪个模块"的字段，但表里不许写逻辑 | 热更变高危 |
| **R6 事件可取消** | 订阅者可 `cancel` 传播，但必须记录到战斗日志 | 异步下无法复盘 |

**通信例**（异常模块与压制模块的协作）：

```
S04 异常九态  --publish-->  「ailment.triggered」{ target, type, accumulated }
S03 压制      <--subscribe-- 收到后：失衡槽 +15%，写日志
（S03 被拆除时，S04 照常发布事件，只是没人订阅 —— S04 不需要任何 if S03 exists 的判断）
```

---

## 11.4 无硬编码规范（★核心★）

### N1 常量引用制

**任何跨模块复用的数值，必须来自常量表，以 `C.域.名` 引用。**

| 写法 | 判定 |
|---|---|
| `damage = atk * 400 / (400 + def)` | ❌ 硬编码常数 400 |
| `damage = atk * C.BATTLE.DEF_CONST / (C.BATTLE.DEF_CONST + def)` | ✅ |
| `if (critRate > 0.45) critRate = 0.45 + (critRate-0.45)*0.4` | ❌ 两处硬编码 |
| `cap = C.BATTLE.CRIT_SOFT_CAP; k = C.BATTLE.CRIT_OVERFLOW_FACTOR` | ✅ |

**例外**：仅在**同一模块内部、且不跨版本调参**的临时中间量，可以写字面量，但必须同行注释 `// 局部量，非调参点`。

### N2 枚举化

一切"种类"必须是**枚举 ID + 数据表**，不许是字符串字面量或裸数字。

| 域 | 枚举示例 |
|---|---|
| 职业 | `CLS.WARRIOR` … `CLS.ARCHER`（14 项） |
| 武器 | `WPN.GREATSWORD` … `WPN.LONGBOW`（14 项） |
| 属性 | `ELM.FLAME / FROST / THUNDER / WIND / STONE / RADIANT / SHADE`（7 项） |
| 异常 | `AIL.CORROSION / PARALYSIS / SLEEP / BLAST / ARMOR_BREAK / MIRE / FROSTBIND / THUNDERSTRIKE / WINDEROSION`（9 项） |
| 部位 | 每个 Boss 部位一个 ID，取自部位矩阵表 |
| 射程带 | `RANGE.MELEE / MID / FAR` |
| 攻击模式 | `MODE.SINGLE / MULTI / HYBRID` |
| 战技位 | `SLOT.MAIN / SUB / SAFE` |

### N3 表驱动

**分支数量 ≥ 3 的逻辑，一律改成查表。** 判据：如果你在写 `switch(职业)` 或 `if (weapon == "巨剑")`，停下来改成查表。

| 反模式 | 正模式 |
|---|---|
| `if (cls == 战士) res *= 1.25` | `res *= table.weapon_affinity[cls][wpn].resource_gain` |
| `switch(ailment) { case 腐蚀: ... }` | `ailment_table[id].on_trigger(params)` |
| 每个职业写一份结算函数 | 一份通用结算 + 职业数据表里的**资源规则描述** |

### N4 条件 DSL（把 if 变成数据）

职业的"资源到阈值就自动做什么"这类规则，写成**数据化的条件表达式**，而不是代码分支：

```yaml
# 战前预案（玩家在港里设的一条规则），存进预设快照
- id: guard_auto_discharge
  when: { op: ">=", left: "$resource.pressure", right: "$C.CLASS.ARTIFICER.VENT_THRESHOLD" }
  then: { action: "vent", slot: "SAFE" }
```

**收益**：新增职业不需要新增代码分支；玩家预案可跨职业复用；托管 AI 与在线玩家走**同一套**条件求值器（托管/在线 TTK 比 ≤1.35 才有保障）。

### N5 内核零内容 ID

`C01–C06` 中**不允许出现任何** `CLS.*` / `WPN.*` / `AIL.*` / 具体 Boss ID。
自动化检查：对内核源码做正则扫描，命中任一内容枚举即 CI 失败。

### N6 数值三层分离

| 层 | 放什么 | 例子 |
|---|---|---|
| **常量表**（跨模块共享的系数/上限/阈值） | 公式常数、全局上限、平衡红线 | `C.BATTLE.DEF_CONST = 400` |
| **数据表**（内容专属数值） | 每职业/武器/Boss 的个体数值 | 战士蓄力阶 M = 1.2/2.0/3.0/3.2 |
| **运行时派生** | 由前两者算出，不存储 | `K = 400/(400+DEF)`、`G×T` |

**禁止**：把派生值预先算好写死进数据表（改常量会失效）；把内容数值写进常量表（加职业要改常量）。

### N7 反例对照（一句话记住）

> **"数字只住在一个地方。"** 一个数字如果出现两次，其中一处必然是引用；一处是 `C.*` 常量，一处是数据表字段，二者不重叠。

---

## 11.5 拆除演练矩阵（每个模块都要能通过）

拆除任一 L1 模块后，系统必须通过三项检查：

| 检查 | 判据 |
|---|---|
| **① 不崩溃** | 冷启动 + 跑完一场标准战斗，无异常、无空指针、无除零 |
| **② 不死锁** | 战斗必然在 `C.BATTLE.TTK_HARD_CAP` 回合内结束；玩家资源条不会卡在无解状态 |
| **③ 数值自动重算** | 依赖该模块的派生值（队伍系数、EHP、TTK、产出期望）按**补偿公式**重算，而非沿用旧值 |

**关键补偿公式**（常量化，见 `12_全局常量表.md` §E）：

| 拆除 | 补偿 |
|---|---|
| `S12 同行兽` | `敌人EHP ×= C.COMPANION.EHP_SCALE`（1.6 → 1.1 队伍系数的等价比） |
| `S04 异常` | `敌人EHP ×= C.AILMENT.EHP_SCALE`（异常乘区失效的输出缺口） |
| `S03 压制` | `绝技期望窗口数 −1`，`绝技充能速率 × C.OVERRIDE.CHARGE_COMP` |
| `S07 绝技` | `战技位 2 → 3`，补齐行动选择空间 |
| `S15 掉落` | 成长改为任务直给，`发放量 × C.DROP.DIRECT_GRANT_RATE` |

> **演练方式**：CI 中对 22 个模块做「逐一拆除 + 跑 1,000 场模拟战斗」的自动化回归，任一模块拆除后崩溃率 >0 即阻断发布。

---

## 11.6 数据表 Schema（要点）

| 表 | 主键 | 关键字段 |
|---|---|---|
| 职业表 | `CLS.*` | 偏好武器、专属资源定义（min/max/自然增长/受击变化/清零规则）、本命词条、流派加成表、生态位标签 |
| 武器表 | `WPN.*` | 模式、段数、基础 ATK、射程带、节奏、`G`（每次注入 RU）、`T`（节奏系数）、特殊占用回合 |
| **基础动作表** | `ACT.<WPN>.<n>` | 每个武器的**全部基础动作**：名称、倍率 M、段数、占回合、异常值 V、破坏槽贡献、资源注入、位移 |
| **战技表** | `SKL.<CLS>.<n>` | 每职业战技全量：战技位、倍率、附加效果、消耗、前置条件（DSL）、变体 |
| 绝技表 | `ARC.*` | 类别（战斗/游击/防御）、倍率或效果量、充能消耗、释放条件 DSL |
| 异常表 | `AIL.*` | 基准阈值 T、衰减 D、触发效果、持续、递增系数、每场上限、直伤类型 |
| 部位矩阵表 | `BOSS.*.part` | 物理 H、七属性 E、破坏阈值、破坏收益、末端/核心/机动标记 |
| Boss 表 | `BOSS.*` | EHP、DEF、SPD、阶段数、意图池、抗性表、异常缩放系数 |
| 掉落表 | `DROP.*` | 来源、素材池、权重、保底规则（**保底次数与数量均为常量引用**） |

**所有表的字段值只允许两种形态**：字面数值（内容专属）/ `$C.域.名`（常量引用）。出现第三种（如写死的职业名）即视为硬编码违规。

---

## 11.7 数值卡字段契约（14 张职业卡共同遵守）

14 张职业数值卡（`13_职业数值卡/01…14`）**必须**包含以下 9 段，顺序一致，字段名一致：

| # | 段 | 必含内容 |
|---|---|---|
| 1 | 职业概览 | 中英文名、emoji、指令、定位、节奏、偏好武器、生态位一句话 |
| 2 | 专属资源 | 名称、区间、获取途径（逐条）、消耗途径（逐条）、RU 换算、自然增长、受击变化、清零规则、**死锁自检** |
| 3 | **基础动作表（全量）** | 偏好武器的**全部**基础动作：M / 段数 / 占回合 / 异常值 V / 破坏槽 / 资源注入 / 位移 / 备注 |
| 4 | **战技表（全量）** | 全部战技：战技位（主/副/保底）、M、附加效果、消耗、前置 DSL、流派变体 |
| 5 | 绝技表 | 3 类至少各 1 个，含倍率/效果量、充能、释放条件 DSL |
| 6 | 本命词条 | 偏好武器下额外生效的**唯一一条**词条 |
| 7 | 五流派适配 | 每流派：加成机制、DPR 期望、得/失、推荐度 |
| 8 | **14 武器适配矩阵** | 该职业 × 14 武器的逐项档位（本命/同源/近邻/跨域）、RU/回合、战技位、走法一句话 |
| 9 | 平衡自检表 | DPR 带、登场率、TTK 比、托管可玩性、资源死锁、与其他职业的差异化证明 |

> 数值卡中**所有**跨模块共享数字必须以 `$C.域.名` 形式引用常量表；仅职业专属数值可直接写数字。

---

## 11.8 自动化体检清单

| # | 检查 | 红线 | 触发动作 |
|---|---|---|---|
| 1 | 硬编码扫描 | 内核命中内容枚举 = 0；字面数字重复出现 >1 次 = 0 | CI 阻断 |
| 2 | 模块逐一拆除回归 | 崩溃率 = 0；死锁率 = 0 | CI 阻断 |
| 3 | 196 组合可玩性 | 每组合存在至少 1 条可行循环 | 告警 |
| 4 | 职业 DPR 带 | 0.92 – 1.08（相对全职业均值） | 调倍率 |
| 5 | 武器层本命组占比 | 45% – 70% | <45% 提本命吸引力；>70% 抬非本命独有价值 |
| 6 | 登场率 | 4% – 18%（14 职业均分 7.14%） | 调生态位 |
| 7 | 托管/在线 TTK 比 | ≤ 1.35 | 补托管预案 |
| 8 | 资源死锁 | 196 组合 × 5 流派，无"资源耗尽且无恢复手段"状态 | 阻断 |
| 9 | 常量孤儿检查 | 常量表中无模块引用的条目 = 0 | 清理 |
| 10 | 经济通缩 | 金币产耗比 1 : 1.10–1.20 | 调强化费 |
| 11 | 通律一 · 频率复算（`14` 号 §三） | 新增/修改带「加速」「+N 段」「频率」「间隔 −N」字样的条目，未重跑**完整循环** ΣM/回合并与职业基准线对比 = 0 | 阻断 |

---

## 11.9 一句话总结

> **内核不知道自己在做一个天空讨伐游戏，模块之间不认识彼此，数字只住在常量表和数据表里。**
> 做到这三点，"拆掉任何一个非核心功能都没问题"就不是承诺，而是架构的自然结果。
