# 模块边界卡：<模块名>

> **用法**：每个模块一张卡，与模块文件同目录或集中归档。「不负责」一栏是本卡价值所在，不得为空。
> **原则**：边界比名字重要；一个职责只能有一个归属方。
> **关联**：[../references/02-拆模块.md](../references/02-拆模块.md)

---

**模块名**：
**所属层**：L0 内核 / L1 系统 / L2 内容
**创建日期**：
**状态**：设计中 / 已实现 / 已冻结

---

## 负责

<!-- 本模块独占的职责。若某项职责有两个模块都声称负责 → 边界未划清，回去重拆。 -->

-
-

## 不负责 ★

<!-- 明确划出去的职责。这一栏决定 AI 会不会「顺手做完」把边界写乱。 -->

-
-

## 依赖

<!-- 它调用谁 / 谁调用它 / 数据来源 -->

**依赖（它调用）**：
| 模块 / 数据 | 用途 | 调用方式（接口 / 事件 / 直读） |
|---|---|---|
|  |  |  |

**被依赖（谁调用它）**：
| 模块 | 用途 | 暴露方式 |
|---|---|---|
|  |  |  |

## 数据字段

<!-- ★ 先于逻辑清单产出。数据没拆，逻辑必然写死。 -->

| 字段 | 类型 | 说明 | 来源（常量键） |
|---|---|---|---|
|  |  |  | `$C.` |

## 逻辑清单

-
-

## 通信约定

<!-- 唯一的写入口 + 只读监听。禁止互相直接改数据。 -->

```
<本模块>:
  <状态字段>
  <写入口方法>
  <变更事件>

<消费方>:
  listen <变更事件> → 只读刷新
```

## 逻辑层 / 表现层切分

| 逻辑层（本模块负责） | 表现层（不归本模块） |
|---|---|
|  |  |

## 异步相关（本项目类型必填）

| 问题 | 约定 |
|---|---|
| 玩家离线时本模块如何执行 |  |
| 超时后的代打策略 |  |
| 状态是否持久化、如何跨天续 |  |
| 托管结果与手动结果是否等价 |  |

## 自检

- [ ] 「负责」无与其他模块重叠
- [ ] 「不负责」非空
- [ ] 数据字段先于逻辑清单填写
- [ ] 有唯一写入口，无外部直接改数据
- [ ] 逻辑层与表现层已切开
- [ ] 异步四项已回答
