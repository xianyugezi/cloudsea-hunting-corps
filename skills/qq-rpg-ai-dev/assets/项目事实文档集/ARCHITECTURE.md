# ARCHITECTURE · 模块职责与公共接口

> 记：模块的**负责 / 不负责 / 依赖**，以及公共接口。
> ★ **这份文档最怕旧**。代码改了这里没改，下一个 AI 会非常认真地按错误信息开发。
> 维护规则：**增量更新**，AI 不许重写整份；代码与本文冲突时**先列冲突，由人判定**。

---

## 分层

| 层 | 职责 | 约束 |
|---|---|---|
| L0 内核 | 与内容无关的通用机制 | ★ **禁止出现任何具体内容 ID** |
| L1 系统 | 可插拔功能模块 | 拆掉任一模块，其余不崩 |
| L2 内容 | 纯数据 | 不含逻辑 |

## 模块边界

<!-- 每个模块一段，三要素缺一不可。可直接用 assets/模块边界卡模板.md。 -->

### <模块名>

**负责**：
-

**不负责**：
-

**依赖**：

## 公共接口

<!-- 接口提前定死，后续所有模块围绕它开发，不要各自重新设计。 -->

```
```

## 常量表

<!-- 指向常量表位置与组织方式。维护规则：只能新增键，不能改名/删除。 -->

| 域 | 内容 |
|---|---|
|  |  |

## 数据流向

```
```

## 禁用模式

<!-- 写明本项目明确不采用的写法及原因。防止 AI 反复提同一个已被否决的方案。 -->

| 模式 | 为什么不用 |
|---|---|
|  |  |
