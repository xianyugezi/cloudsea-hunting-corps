# 发布到 GitHub 说明

当前环境（沙箱）**无 GitHub 网络出口**，已无法由 AI 直接建库/推送。
本地 git 仓库已完成初始提交（`main` 分支，53 个文件），可直接在你联网的机器上发布。

## 方式一：一键脚本（推荐）
在**本仓库目录**下、能联网且装了 `gh` 的机器上执行：

```bash
GH_TOKEN=ghp_你的令牌 bash 发布到GitHub.sh
```

脚本会自动：登录 → 创建公开库 `cloudsea-hunting-corps` → 推送 `main` → 打印仓库 URL。
若仓库已存在，则跳过创建直接推送。

## 方式二：手动命令
```bash
cd <本仓库目录>
echo ghp_你的令牌 | gh auth login --with-token
gh repo create cloudsea-hunting-corps --public \
  --description "云海猎团 — QQ群异步回合制 RPG·PvE 设计稿" \
  --source . --remote origin --push
```

## 说明
- 仓库名 `cloudsea-hunting-corps`，可见性 Public，owner 为令牌对应账号。
- 令牌需 `repo`（或 Fine-grained 的 Repository creation + Contents 读写）权限。
- 仓库内容：00–15 章设计文档、`13_职业数值卡/`、`skills/qq-rpg-ai-dev/`、
  `build_collection.py`、`云海猎团_设计稿合集.md`（单文件编译快照）。
- 后续更新：改完文档跑 `python3 build_collection.py` 后 `git commit -am "..." && git push` 即可。
