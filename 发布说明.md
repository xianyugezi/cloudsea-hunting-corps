# 发布到 GitHub 说明

仓库已创建并完成首次推送，后续同步用一键脚本即可。

- **仓库**：https://github.com/xianyugezi/cloudsea-hunting-corps （公开，英文名）
- **当前远程状态**：`main` 分支，与本地 git 历史一致（含 333813a 等全部提交）

## 一键同步（推荐）

在**本仓库目录**下执行（任何能联网的机器都行；令牌只在本进程使用，不落盘）：

```bash
export PAT=ghp_你的令牌
bash 发布到GitHub.sh
```

脚本会自动：
1. 探测 github 域名连通性——若被受限出口的 DNS 封锁，自动写 `/etc/hosts` 直连真实 IP；
2. 仓库不存在则创建公开库 `cloudsea-hunting-corps`（已存在则跳过）；
3. 用 `TLS1.3 + 请求头注入令牌` 推送 `main`，并**校验远程 sha 与本地一致**（最多重试 8 次）。

## 手动命令（等价操作）

```bash
cd <本仓库目录>
git remote add origin https://github.com/xianyugezi/cloudsea-hunting-corps.git
# 每次推送时注入令牌（避免明文写进 .git/config）：
git -c http.sslVersion=tlsv1.3 \
    -c "http.https://github.com/.extraheader=AUTHORIZATION: basic $(printf 'x-access-token:%s' "$PAT" | base64)" \
    push -u origin main
```

## 说明

- 令牌需 `repo`（或 Fine-grained 的 Repository creation + Contents 读写）权限。
- 仓库内容：00–15 章设计文档、`13_职业数值卡/`、`15_主线章节与任务/`、
  `skills/qq-rpg-ai-dev/`、`build_collection.py`、`云海猎团_设计稿合集.md`（单文件编译快照）。
- 后续更新流程：改完分章文档 → `python3 build_collection.py` 重建合集 → commit → 运行本脚本。
- ⚠️ 本次使用的个人访问令牌曾在对话中以明文出现，**用完后请到 GitHub 设置 → Developer settings → Personal access tokens 吊销并重新生成**。
